function faca() {
    var n = Number(prompt('Digite um número inteiro qualquer'))
    var antecessor = (n - 1)
    var sucessor = (n + 1)
    alert (`antecessor: ${ antecessor}\nsucessor: ${sucessor}`)
}